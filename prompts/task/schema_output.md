---
name: schema_output
description: Appended to a task subagent's assignment when the item carries an outputSchema, requiring a JSON final message.
---
Your FINAL message MUST be exactly one JSON document satisfying this JSON Schema — no prose, no markdown fences, nothing else:

{{schema}}
