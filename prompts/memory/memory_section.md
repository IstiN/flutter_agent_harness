---
name: memory_section
description: >
  Renders the agent's long-term memory entries into the system prompt so the
  model has durable cross-session context. Entries are pulled from the project
  and user scopes and capped at ~2 KiB.
---

{{memorySection}}
