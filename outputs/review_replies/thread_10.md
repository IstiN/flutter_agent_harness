# Re: 🔵 SUGGESTION (still open from round 1): truncated selected rows lose the accent wrap

Fixed this round — same change as the round-1 thread on this file: the truncated branch of `_menuItemRow` wraps the fitted plain label in `_rearmSelection`, and the stale no-op comment is replaced (details + the new RED→GREEN regression test in `outputs/review_replies/thread_3.md`).
