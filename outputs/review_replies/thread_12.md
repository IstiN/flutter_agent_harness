# Re: 🔵 SUGGESTION (still open from round 1): stale "byte-identical" doc on `kDefaultTuiTheme`

Fixed this round — same change as the round-1 thread on this file: the doc comment now states the explicit `toolOutput` foreground was added in gh-671 and the old byte-identical invariant is deliberately broken (text in `outputs/review_replies/thread_5.md`).
