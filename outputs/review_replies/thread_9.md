**Fixed.** The 3:1 label-floor matrix in `test/cli/tui_theme_test.dart` covers `toolTitle` on **both** `toolSuccessBg` and `toolErrorBg` for every built-in palette (mirroring the per-tint glyph-rail checks), so the gap this thread describes — a palette passing on the error tint but failing on the success tint — can no longer slip through. All 7 palettes clear the floor on both tints; re-verified green this round after the `main` merge (65 tests in the three touched suites).
