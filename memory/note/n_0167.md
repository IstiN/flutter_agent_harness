---
id: "n_0167"
type: "note"
title: "fa v0.1.225 (commit 612f8f7): input caret stays VISIBLE while a run streams — fa_tui view() hideCursor = pickerOpen only (was busy||pickerOpen). The old busy-hide guarded a pre-force-home artifact; renderer (_cursor_state.apply forceHome:wroteRows/wroteCells) now re-homes after EVERY painting frame, so busy-hide just hid the user's typing. Tests updated: fa_tui_test 'physical cursor stays in the input while busy' + spinner trailing-line expects \x1b[?25h; pickers/prompts still hide. Card history this session: 0.1.221 no-newline rewrap fix (typing lag ×288), 0.1.222 busy Ctrl+C hint, 0.1.223 Ctrl+C=/exit + input-frame fps-throttle bypass (echo 12ms→0.2ms), 0.1.224 cross-project messaging root routing (send resolves recipient slug via messages-registry.json + sibling scan), 0.1.225 caret visible. Remaining known: ~264ms one-off build loading huge sessions (lazy wrap / tail-first replay TODO), compaction of 200k+ tokens takes minutes (chunked summarization TODO)."
author: "agent"
date: "2026-08-27T20:37:17.381898Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "v0.1.225", "cursor", "tui"]
---


# Note: n_0167

fa v0.1.225 (commit 612f8f7): input caret stays VISIBLE while a run streams — fa_tui view() hideCursor = pickerOpen only (was busy||pickerOpen). The old busy-hide guarded a pre-force-home artifact; renderer (_cursor_state.apply forceHome:wroteRows/wroteCells) now re-homes after EVERY painting frame, so busy-hide just hid the user's typing. Tests updated: fa_tui_test 'physical cursor stays in the input while busy' + spinner trailing-line expects \x1b[?25h; pickers/prompts still hide. Card history this session: 0.1.221 no-newline rewrap fix (typing lag ×288), 0.1.222 busy Ctrl+C hint, 0.1.223 Ctrl+C=/exit + input-frame fps-throttle bypass (echo 12ms→0.2ms), 0.1.224 cross-project messaging root routing (send resolves recipient slug via messages-registry.json + sibling scan), 0.1.225 caret visible. Remaining known: ~264ms one-off build loading huge sessions (lazy wrap / tail-first replay TODO), compaction of 200k+ tokens takes minutes (chunked summarization TODO).

**By:** [[agent]]
**Date:** 2026-08-27T20:37:17.381898Z
**Area:** [[project|project]]
