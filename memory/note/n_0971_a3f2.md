---
id: "n_0971_a3f2"
type: "note"
title: "Issue #355 SSH freeze: fa v0.1.377 predates the #356 fix — stale binary on the Mac mini (agents@192.168.1.11), not a code regression. Fixed by installing v0.1.384."
author: "agent"
date: "2026-09-14T23:40:00.000000Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.8
tags: ["#note", "#source_agent", "issue-355", "shift-hid", "release-versions"]
---


# Note: n_0971_a3f2

Issue #355 (TUI freeze on first Enter over SSH, Ctrl+C dead): the fix (PR #356, commit 6ffeb9d4 — SSH env gate + FA_TUI_SHIFT_HID kill switch + sacrificial probe isolate in `lib/src/cli/shift_hid.dart`) is correct and complete. The user's Mac mini (agents@192.168.1.11) kept freezing because its installed fa 0.1.377 (tag at 9a56edc3, cut 2026-09-14 13:55) PREDATES the fix — `git merge-base --is-ancestor 6ffeb9d4 v0.1.377` is false. The issue's "post-error path" forensic theory (turn-end subscriber wedge) was a misdiagnosis: PTY matrix with a proper controlling tty shows 0.1.377 wedges 3/3 (keystroke echo +0 bytes after Enter = main blocked in CG FFI) and v0.1.384 is healthy 3/3 (+15 bytes echo, Ctrl+C exits 130). Fix on the box = install release v0.1.384 bundle to ~/.local/bin (backup fa-0.1.377.bak).

Pitfalls burned time here: (1) a PTY driver using setsid without a controlling tty (TPGID -1) makes isig Ctrl+C vanish — SIGINT never fires, false "Ctrl+C dead" verdicts; use pty.fork() so the child owns the tty. (2) fa.log diagnostic lines from simultaneously-running wedged zombies can garble/interleave — don't trust partial-line forensic reads. (3) `dart compile exe` refuses this repo (sqlite3 build hooks) — verify from source via `dart bin/fah.dart` (JIT), or use the GitHub release bundle.

**By:** [[agent]]
