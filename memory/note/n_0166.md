---
id: "n_0166"
type: "note"
title: "fa v0.1.224 CROSS-PROJECT MESSAGING FIX: messaging fabric root = <sessionRoot>/<cwd-slug>/messages (NOT ~/.fah/messages — that unified-root memory note does NOT match shipped 0.1.223 code). agent_message wrote into the SENDER's slug root → a peer fa in ANOTHER project (draining only its own root) never received mail (live-reproduced: a profile-feedback message to the crap4dart agent sat in flutter_agent's root; bridge = cp the inbox json into the recipient slug's messages/<id>_main/inbox/). Fix: FileMessagingRepository.send resolves the recipient root via ~/.fah/messages-registry.json (agentId(raw)->slug map, written by register()) then broad sibling-slug scan by .id marker; unknown ids stay local. Tests: cross-project send routing group in file_messaging_repository_test.dart."
author: "agent"
date: "2026-08-27T20:31:29.811269Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "v0.1.224", "messaging", "cross-project", "bug"]
---


# Note: n_0166

fa v0.1.224 CROSS-PROJECT MESSAGING FIX: messaging fabric root = <sessionRoot>/<cwd-slug>/messages (macOS: ~/Library/Group Containers/group.dev.fa1.shared/fa/sessions/--<slug>--/messages/; NOT ~/.fah/messages — that unified-root memory note does NOT match shipped 0.1.223 code). agent_message wrote into the SENDER's slug root → a peer fa in ANOTHER project (draining only its own root) never received mail (live-reproduced: my profile-feedback message to the crap4dart agent sat in flutter_agent's root; bridge = cp the inbox json into the recipient slug's messages/<id>_main/inbox/). Fix: FileMessagingRepository.send resolves the recipient root via ~/.fah/messages-registry.json (agentId(raw)->slug map, written by register()) then broad sibling-slug scan by .id marker; unknown ids stay local. Tests: cross-project send routing group in file_messaging_repository_test.dart. crap4dart agent mailbox: 01a044d9-e7e5-7b61-a797-789dc2a4f9a3/main (cwd git/crap4dart); its profile command total column still over-attributes on recursive/nested calls — reported to that agent via mail.

**By:** [[agent]]
**Date:** 2026-08-27T20:31:29.811269Z
**Area:** [[project|project]]
