---
id: "n_0223"
type: "note"
title: "WORKFLOW CHANGE (user directive): work ONLY in the main checkout of the flutter_agent repo — NO more temporary clones. All changes must land there. Operational facts: (1) the main checkout's remote is `origin` (https://github.com/IstiN/flutter_agent_harness), NOT `gh` — temporary clones used `gh`; push = `git push origin HEAD:main`. (2) The checkout carries OTHER agents' uncommitted work (flutter_app SKILL.md + demo tests were modified; junk files .cat.txt/.ct*/.push.log etc.) — stash before pull --rebase, pop after; their rebase may refuse on fa-local/bundle/bin/fah dirt (checkout or restore it first). (3) The macOS app build failed on main because the app resolved a STALE lock (harness 0.1.218!) and app-side wiring for 0.1.233 shipped incomplete: fixed in 0.1.239 — barrel exports for ScheduledMessageQueue/scheduleMessageTool (lib/flutter_agent_harness.dart), `late final ScheduledMessageQueue _scheduledMessages` field + unawaited(start()) in agent_service.dart, FileMessagingRepository root is plain String (NOT factory — only ScheduledMessageQueue takes factories), _AutoCompactorFlutterHooks.onDelta override, flutter_app pubspec flutter_agent_memory ^0.0.6→^0.1.1. flutter analyze app: 13 pre-existing infos, 0 errors."
author: "agent"
date: "2026-08-28T09:01:09.659519Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "workflow", "main-checkout", "no-clones", "origin-remote"]
---


# Note: n_0223

WORKFLOW CHANGE (user directive): work ONLY in the main checkout /Users/Uladzimir_Klyshevich/git/flutter_agent — NO more /tmp clones. All changes must land there. Operational facts: (1) the main checkout's remote is `origin` (https://github.com/IstiN/flutter_agent_harness), NOT `gh` — the /tmp clone used `gh`; push = `git push origin HEAD:main`. (2) The checkout carries OTHER agents' uncommitted work (flutter_app SKILL.md + demo tests were modified; junk files .cat.txt/.ct*/.push.log etc.) — stash before pull --rebase, pop after; their rebase may refuse on fa-local/bundle/bin/fah dirt (checkout or restore it first). (3) The macOS app build failed on main because the app resolved a STALE lock (harness 0.1.218!) and app-side wiring for 0.1.233 shipped incomplete: fixed in 0.1.239 — barrel exports for ScheduledMessageQueue/scheduleMessageTool (lib/flutter_agent_harness.dart), `late final ScheduledMessageQueue _scheduledMessages` field + unawaited(start()) in agent_service.dart, FileMessagingRepository root is plain String (NOT factory — only ScheduledMessageQueue takes factories), _AutoCompactorFlutterHooks.onDelta override, flutter_app pubspec flutter_agent_memory ^0.0.6→^0.1.1. flutter analyze app: 13 pre-existing infos, 0 errors.

**By:** [[agent]]
**Date:** 2026-08-28T09:01:09.659519Z
**Area:** [[project|project]]
