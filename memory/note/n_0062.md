---
id: "n_0062"
type: "note"
title: "CTX-OVERFLOW ROUND HANDOFF (branch perf/ctx-guard commit 06b6b34, base 803e90d v0.1.213): USER BUG 'session widgets ctx 240% (480k/200k)' ROOT-CAUSED+FIXED+REPRODUCED 1:1 (my repro showed 'ctx 182% (182k/100k)'). Root cause: auto-compaction only ran in _afterRun (post-settle); a single failure (quota-limited smol role) or mid-run growth left every NEXT request knowingly over-window while gauge honestly displayed >100%. FIX: _runPrompt now calls _maybeAutoCompact() before each fresh user turn (skips internal auto-continue) reusing proven pipeline/thresholds; analyze clean on touched files. E2E positive assertion SKIP'd (in-test tracker text) due to NEW INDEPENDENT BUG discovered & bisected with DBG markers: SECOND-process boot over ~730KB session JSONL replays fine ('restored session' seen) then STALLS BEFORE _handleLine ever receives typed input (no hLine/startRun/runPrompt DBG markers; idle prompt; turn 0). Not related to guard. Repro recipe lives in the skipped test body + earlier ctx_boot_probe pattern (MemoryExecutionEnv, seed named session via '/session big'+'seed', direct repo.appendMessage×26×28k chars, boot config sessionName:'big', waitForIt 'restored session', sendLine('hi') → hang). ENVIRONMENT WAR-STORY: cross-agent bash stdout mixing AND /tmp filename collisions persisted ALL DAY even after user killed agents — a live fa session 'stealth/ox-alpha' cwd=/private/tmp kept surfacing inside MY echoes and MY log files got interleaved with agent_cli_test runs from MAIN checkout (someone else ran copies of my tests there, incl broken TX tests -> unmodifiable-list at ansi_markdown.dart:694 and '_publishCleanViews undefined' = THEIR half-stripped copy, NOT worktree state). Therefore: NO PUSH TO MAIN this round (nothing shipped unverified); next quiet window: (1) rerun scoped suites in solo env incl transcript suites on merged base — verify TX engine intact post v0.1.212/213 (suspicious counters 12420 vs <650 seen in foreign logs may indicate THEIR stripped copy, must confirm on real main); (2) fix giant-resume stall bug; (3) unskip over-window e2e + assert ordering auto-compacted<answer; (4) THEN push merge to main and rebuild local fa; (5) continue typing-latency hunt via live two-terminal yoloit debugging per user note."
author: "agent"
date: "2026-08-27T07:32:55.469883Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa-cli", "ctx-bug", "handoff", "two-new-bugs"]
---


# Note: n_0062

CTX-OVERFLOW ROUND HANDOFF (branch perf/ctx-guard commit 06b6b34, base 803e90d v0.1.213): USER BUG 'session widgets ctx 240% (480k/200k)' ROOT-CAUSED+FIXED+REPRODUCED 1:1 (my repro showed 'ctx 182% (182k/100k)'). Root cause: auto-compaction only ran in _afterRun (post-settle); a single failure (quota-limited smol role) or mid-run growth left every NEXT request knowingly over-window while gauge honestly displayed >100%. FIX: _runPrompt now calls _maybeAutoCompact() before each fresh user turn (skips internal auto-continue) reusing proven pipeline/thresholds; analyze clean on touched files. E2E positive assertion SKIP'd (in-test tracker text) due to NEW INDEPENDENT BUG discovered & bisected with DBG markers: SECOND-process boot over ~730KB session JSONL replays fine ('restored session' seen) then STALLS BEFORE _handleLine ever receives typed input (no hLine/startRun/runPrompt DBG markers; idle prompt; turn 0). Not related to guard. Repro recipe lives in the skipped test body + earlier ctx_boot_probe pattern (MemoryExecutionEnv, seed named session via '/session big'+'seed', direct repo.appendMessage×26×28k chars, boot config sessionName:'big', waitForIt 'restored session', sendLine('hi') → hang). ENVIRONMENT WAR-STORY: cross-agent bash stdout mixing AND /tmp filename collisions persisted ALL DAY even after user killed agents — a live fa session 'stealth/ox-alpha' cwd=/private/tmp kept surfacing inside MY echoes and MY log files got interleaved with agent_cli_test runs from MAIN checkout (someone else ran copies of my tests there, incl broken TX tests -> unmodifiable-list at ansi_markdown.dart:694 and '_publishCleanViews undefined' = THEIR half-stripped copy, NOT worktree state). Therefore: NO PUSH TO MAIN this round (nothing shipped unverified); next quiet window: (1) rerun scoped suites in solo env incl transcript suites on merged base — verify TX engine intact post v0.1.212/213 (suspicious counters 12420 vs <650 seen in foreign logs may indicate THEIR stripped copy, must confirm on real main); (2) fix giant-resume stall bug; (3) unskip over-window e2e + assert ordering auto-compacted<answer; (4) THEN push merge to main and rebuild local fa; (5) continue typing-latency hunt via live two-terminal yoloit debugging per user note.

**By:** [[agent]]
**Date:** 2026-08-27T07:32:55.469883Z
**Area:** [[project|project]]
