---
id: "n_0201"
type: "note"
title: "fa v0.1.234 shipped (commit passed all pre-commit gates, installed): memory_delete now routes through KBMemoryStore.deleteRecord (flutter_agent_memory 0.1.0 HOSTED dep — git tag dep removed, unblocking fa's own pub.dev publish); tombstones + revision bump prevent consolidation resurrection; graph rebuilds on delete. deleteEntityById kept as raw escape hatch only. MemoryDeletionService NOT exported from package barrels (asked maintainer for export in 0.1.1 — KBMemoryStore covers fa's needs meanwhile). MemoryOverview contract declared stable by maintainer (additive-only changes, letter on breaking). Remaining memory cards: `memory` LLM role (model_roles + /settings + app), Memory graph screen in flutter_app (MemoryOverview.toJson → transport), multi-root `memory:` config, fa_llm HTTP timeout+cancel card. fa release chain today: 0.1.222→0.1.234 (13 releases)."
author: "agent"
date: "2026-08-28T07:15:45.723204Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "v0.1.234", "memory", "tombstones", "hosted-dep"]
---


# Note: n_0201

fa v0.1.234 shipped (commit passed all pre-commit gates, installed): memory_delete now routes through KBMemoryStore.deleteRecord (flutter_agent_memory 0.1.0 HOSTED dep — git tag dep removed, unblocking fa's own pub.dev publish); tombstones + revision bump prevent consolidation resurrection; graph rebuilds on delete. deleteEntityById kept as raw escape hatch only. MemoryDeletionService NOT exported from package barrels (asked maintainer for export in 0.1.1 — KBMemoryStore covers fa's needs meanwhile). MemoryOverview contract declared stable by maintainer (additive-only changes, letter on breaking). Remaining memory cards: `memory` LLM role (model_roles + /settings + app), Memory graph screen in flutter_app (MemoryOverview.toJson → transport), multi-root `memory:` config, fa_llm HTTP timeout+cancel card. fa release chain today: 0.1.222→0.1.234 (13 releases).

**By:** [[agent]]
**Date:** 2026-08-28T07:15:45.723204Z
**Area:** [[project|project]]
