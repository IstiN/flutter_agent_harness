---
id: "n_0650_9a1b"
type: "note"
title: "fa f76f88ff — truncated-id mail loss fixed at BOTH layers: (1) agent_message resolves a unique id prefix (and `…`-decorated forms) to the full mailbox id, ambiguous prefix = error with candidates (before: 01a060f2/main for 01a060f2-7d4b-…/main silently created a dead 01a060f2_main mailbox no watcher polls — the widgets agent's message sat there for hours); (2) the CLI inbox watcher's reclaimOrphanMailboxMail sweeps orphan <head>_main dirs into the real inbox every 2s tick (heals mail from old binaries). Same commit lands the persisted model cache: ~/.fah/model_cache.json, stale-while-revalidate with 24h TTL, /model picker instant at boot. Note: the CRAP ratchet pins _resolveFabricAddress-family helpers at CC<=12 — extract, don't extend."
author: "agent"
date: "2026-09-06T19:08:09.866466Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "false", "messaging", "bugfix", "f76f88ff"]
---


# Note: n_0650_9a1b

fa f76f88ff — truncated-id mail loss fixed at BOTH layers: (1) agent_message resolves a unique id prefix (and `…`-decorated forms) to the full mailbox id, ambiguous prefix = error with candidates (before: 01a060f2/main for 01a060f2-7d4b-…/main silently created a dead 01a060f2_main mailbox no watcher polls — the widgets agent's message sat there for hours); (2) the CLI inbox watcher's reclaimOrphanMailboxMail sweeps orphan <head>_main dirs into the real inbox every 2s tick (heals mail from old binaries). Same commit lands the persisted model cache: ~/.fah/model_cache.json, stale-while-revalidate with 24h TTL, /model picker instant at boot. Note: the CRAP ratchet pins _resolveFabricAddress-family helpers at CC<=12 — extract, don't extend.

**By:** [[agent]]
**Date:** 2026-09-06T19:08:09.866466Z
**Area:** [[project|project]]
