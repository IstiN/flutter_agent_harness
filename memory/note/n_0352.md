---
id: "n_0352"
type: "note"
title: "Round-2 launcher/catalog fixes (2026-08-30): d617a166 — ROOT CAUSE of 'cannot remove via long tap': _showTileMenu early-returned for any non-demo app WITHOUT a live tile (all catalog widgets) — menu now opens for every app; right-click (onSecondaryTapUp) added to grid/search tiles; soft indigo hoverColor. 0c76c857+2d0ac51a — catalog sheet: installed&current swaps Preview for Remove (red), 'Created by me' section (local apps not in catalog, Open/Remove), palette-based solid avatar fallback, torn duplicate class tail removed; fixture zip manifest now has a version. 7ed623f2 — hold-release test updated to expect the menu. NO .installed.json/entryFor mechanism exists — isDemo = demoAppIds.contains(id). Pomodoro widget: jsr agent (01a052ad-9e88-72e0-b9ff-487b7c1f45b2/main) built it in flutter_js_widget_runtime@a1b3909 (example/widgets/pomodoro); I ported verbatim to fa_widgets@4131e69 (minRuntime 0.4.79, icon.svg tomato) — live in catalog (18 widgets). Sync agreement: both sides message each other on widget.js changes. Clobber pattern refined: field-declaration edits get reverted within seconds; fix = python patch + analyze + git add + commit in ONE bash chain, then verify via git show HEAD:file | grep."
author: "agent"
date: "2026-08-30T14:58:29.907897Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "launcher", "catalog", "pomodoro", "coordination"]
---


# Note: n_0352

Round-2 launcher/catalog fixes (2026-08-30): d617a166 — ROOT CAUSE of 'cannot remove via long tap': _showTileMenu early-returned for any non-demo app WITHOUT a live tile (all catalog widgets) — menu now opens for every app; right-click (onSecondaryTapUp) added to grid/search tiles; soft indigo hoverColor. 0c76c857+2d0ac51a — catalog sheet: installed&current swaps Preview for Remove (red), 'Created by me' section (local apps not in catalog, Open/Remove), palette-based solid avatar fallback, torn duplicate class tail removed; fixture zip manifest now has a version. 7ed623f2 — hold-release test updated to expect the menu. NO .installed.json/entryFor mechanism exists — isDemo = demoAppIds.contains(id). Pomodoro widget: jsr agent (01a052ad-9e88-72e0-b9ff-487b7c1f45b2/main) built it in flutter_js_widget_runtime@a1b3909 (example/widgets/pomodoro); I ported verbatim to fa_widgets@4131e69 (minRuntime 0.4.79, icon.svg tomato) — live in catalog (18 widgets). Sync agreement: both sides message each other on widget.js changes. Clobber pattern refined: field-declaration edits get reverted within seconds; fix = python patch + analyze + git add + commit in ONE bash chain, then verify via git show HEAD:file | grep.

**By:** [[agent]]
**Date:** 2026-08-30T14:58:29.907897Z
**Area:** [[project|project]]
