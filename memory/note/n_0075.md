---
id: "n_0075"
type: "note"
title: "Busy-phase labels SHIPPED: commit a307344 'feat(tui): truthful busy-row phase labels' on origin/main (GitHub push verified e17d983..a307344), fa v0.1.213 rebuilt+installed from the clean clone. Mechanic: BusyMsg got optional `phase`; an in-busy relabel swaps label over the SAME elapsed window without restarting the tick chain (extra Cmd=null), idle clears it; controller seam FaTuiController.setBusyPhase (renamed from sendBusyPhase during debugging); AgentCli calls it around _runAutoCompact ('Compacting context…') and memoryExtractionHook ('Extracting memory…'). Root cause of user's '⠧ Working… 646s' hang report: _handleTuiSubmit holds sendBusy(true) across await _settled which covers awaited post-run auto-compaction passes (up to 8×retries) + durable-memory extraction now riding the MAIN model, all UI-silent. Two regression tests added in fa_tui_test.dart (label swap keeps busyStartedAtMs; relabels schedule no extra tick Cmd). env hostile-process lesson: ghost replayer can REPLAY edits as duplicates and INJECT false dart-analyze logs into output files mid-flight — ground truth = fresh compile+test runs (analyzer-log errors contradicted by passing tests were fake); clean-room protocol: work in a throwaway git clone (/tmp/fa_phase_check_*), amend+push directly to GitHub from there, bypassing both the shared tree and the rollbacker. User also reported scroll jerkiness during streaming (next perf target alongside typing latency; input-latency agent delivered ranked interventions + keypress→paint JSONL tracer proposal)."
author: "agent"
date: "2026-08-27T08:29:09.539093Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "false"]
---


# Note: n_0075

Busy-phase labels SHIPPED: commit a307344 'feat(tui): truthful busy-row phase labels' on origin/main (GitHub push verified e17d983..a307344), fa v0.1.213 rebuilt+installed from the clean clone. Mechanic: BusyMsg got optional `phase`; an in-busy relabel swaps label over the SAME elapsed window without restarting the tick chain (extra Cmd=null), idle clears it; controller seam FaTuiController.setBusyPhase (renamed from sendBusyPhase during debugging); AgentCli calls it around _runAutoCompact ('Compacting context…') and memoryExtractionHook ('Extracting memory…'). Root cause of user's '⠧ Working… 646s' hang report: _handleTuiSubmit holds sendBusy(true) across await _settled which covers awaited post-run auto-compaction passes (up to 8×retries) + durable-memory extraction now riding the MAIN model, all UI-silent. Two regression tests added in fa_tui_test.dart (label swap keeps busyStartedAtMs; relabels schedule no extra tick Cmd). env hostile-process lesson: ghost replayer can REPLAY edits as duplicates and INJECT false dart-analyze logs into output files mid-flight — ground truth = fresh compile+test runs (analyzer-log errors contradicted by passing tests were fake); clean-room protocol: work in a throwaway git clone (/tmp/fa_phase_check_*), amend+push directly to GitHub from there, bypassing both the shared tree and the rollbacker. User also reported scroll jerkiness during streaming (next perf target alongside typing latency; input-latency agent delivered ranked interventions + keypress→paint JSONL tracer proposal).

**By:** [[agent]]
**Date:** 2026-08-27T08:29:09.539093Z
**Area:** [[project|project]]
