---
id: "n_0182"
type: "note"
title: "fa v0.1.229 (commit f3061e0): memory_delete tool (id-based, scope scan project->user or explicit scope; ExecutionEnvKbStorage.deleteEntityById across question/answer/note; onChanged prompt refresh) — works in CLI AND flutter app via shared memoryTools. Transcript restyles <task-result id=...> (attributes handled), [auto-compacted], [context trimmed], [memory maintained] as dim blockquotes with gear marker (renderSystemNoticeLines in lib/src/cli/system_notice_render.dart; model records keep raw tags). 1006 tests green. Memory architecture facts: storage root injected, per-project <root>/.fah/memory + user scope — 2 scopes already; memory LLM = smol role today (compaction_memory_hook.dart, memory_controller.dart:248), dedicated 'memory' role card pending. Search hang root cause (mem-explore): provider-backed search makes 2 LLM HTTP round trips via fa_llm with NO timeout, no-op cancel(), ignored cancelToken + O(N) full scans — fix assigned to the flutter_agent_memory maintainer agent; that agent is also landing tombstone deletion (MemoryDeletionService + DELETIONS.md) and was asked to expose graph-overview data API publicly for the app Memory screen."
author: "agent"
date: "2026-08-27T21:20:58.219322Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: [fa, v0.1.229, memory, memory-delete, styling]
---


# Note: n_0182

fa v0.1.229 (commit f3061e0): memory_delete tool (id-based, scope scan project->user or explicit scope; ExecutionEnvKbStorage.deleteEntityById across question/answer/note; onChanged prompt refresh) — works in CLI AND flutter app via shared memoryTools. Transcript restyles <task-result id=...> (attributes handled), [auto-compacted], [context trimmed], [memory maintained] as dim blockquotes with gear marker (renderSystemNoticeLines in lib/src/cli/system_notice_render.dart; model records keep raw tags). 1006 tests green. Memory architecture facts: storage root injected, per-project <root>/.fah/memory + user scope — 2 scopes already; memory LLM = smol role today (compaction_memory_hook.dart, memory_controller.dart:248), dedicated 'memory' role card pending. Search hang root cause (mem-explore): provider-backed search makes 2 LLM HTTP round trips via fa_llm with NO timeout, no-op cancel(), ignored cancelToken + O(N) full scans — fix assigned to flutter_agent_memory maintainer agent (mailbox 01a04504-b629-7f09-8739-2251528abaac/main); that agent is also landing tombstone deletion (MemoryDeletionService + DELETIONS.md) and was asked to expose graph-overview data API publicly for the app Memory screen.

**By:** [[agent]]
**Date:** 2026-08-27T21:20:58.219322Z
**Area:** [[project|project]]
