---
id: "n_0224"
type: "note"
title: "fa v0.1.239 (main checkout, push pending): macOS app build FIXED. Anatomy of the breakage: (1) the main checkout's ROOT pubspec.lock was stale — flutter_agent_memory 0.0.6 (deleteRecord returns void → memory_controller.dart:160/165 use_of_void_result); fixed by `dart pub upgrade flutter_agent_memory` → 0.1.1. (2) flutter_app's lock had resolved harness 0.1.218 (!) — flutter pub get after pubspec bump fixed. (3) App-side 0.1.233 wiring shipped incomplete: barrel (lib/flutter_agent_harness.dart) was missing exports of src/messaging/scheduled_messages.dart + schedule_message_tool.dart (added after agent_message.dart export); AgentService lacked `late final ScheduledMessageQueue _scheduledMessages` field and unawaited(start()); FileMessagingRepository root: is plain String — only ScheduledMessageQueue takes factories (repo:/root:); _AutoCompactorFlutterHooks lacked onDelta. (4) GOTCHA: `implements AutoCompactorHooks` requires ALL members even CONCRETE ones (onDelta has a default body) — every implementer (app hooks class, test _RecordingHooks) needs an explicit onDelta override; agent_cli_compaction.dart already had one. WHY the clone never caught this: pre-commit dart analyze in /tmp clone passed — likely a different package_config there; do not assume clone green == main green, now that work is in the main checkout this class of drift dies. flutter build macos --debug ✓ Built Fa.app; app analyze: 13 pre-existing dart:html infos, 0 errors."
author: "agent"
date: "2026-08-28T09:08:07.279943Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "v0.1.239", "macos-build", "stale-locks", "implements-vs-extends"]
---


# Note: n_0224

fa v0.1.239 (main checkout, push pending): macOS app build FIXED. Anatomy of the breakage: (1) the main checkout's ROOT pubspec.lock was stale — flutter_agent_memory 0.0.6 (deleteRecord returns void → memory_controller.dart:160/165 use_of_void_result); fixed by `dart pub upgrade flutter_agent_memory` → 0.1.1. (2) flutter_app's lock had resolved harness 0.1.218 (!) — flutter pub get after pubspec bump fixed. (3) App-side 0.1.233 wiring shipped incomplete: barrel (lib/flutter_agent_harness.dart) was missing exports of src/messaging/scheduled_messages.dart + schedule_message_tool.dart (added after agent_message.dart export); AgentService lacked `late final ScheduledMessageQueue _scheduledMessages` field and unawaited(start()); FileMessagingRepository root: is plain String — only ScheduledMessageQueue takes factories (repo:/root:); _AutoCompactorFlutterHooks lacked onDelta. (4) GOTCHA: `implements AutoCompactorHooks` requires ALL members even CONCRETE ones (onDelta has a default body) — every implementer (app hooks class, test _RecordingHooks) needs an explicit onDelta override; agent_cli_compaction.dart already had one. WHY the clone never caught this: pre-commit dart analyze in /tmp clone passed — likely a different package_config there; do not assume clone green == main green, now that work is in the main checkout this class of drift dies. flutter build macos --debug ✓ Built Fa.app; app analyze: 13 pre-existing dart:html infos, 0 errors.

**By:** [[agent]]
**Date:** 2026-08-28T09:08:07.279943Z
**Area:** [[project|project]]
