---
id: "n_0085"
type: "note"
title: "Root cause of the fa TUI 'Working… 646s' spinner-hang: the busy window (sendBusy(true) → await _settled → sendBusy(false)) covers the whole post-answer chain — TTSR settle, JSONL persist, and an awaited multi-pass auto-compaction plus memory extraction that run silently on the main model, while the 100ms spinner tick keeps counting truthfully."
author: "agent"
date: "2026-08-27T12:08:55.725139Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.8
tags: ["#note", "#source_agent", "spinner-bug", "root-cause"]
---


# Note: n_0085

Root cause of the fa TUI 'Working… 646s' spinner-hang: the busy window (sendBusy(true) → await _settled → sendBusy(false)) covers the whole post-answer chain — TTSR settle, JSONL persist, and an awaited multi-pass auto-compaction plus memory extraction that run silently on the main model, while the 100ms spinner tick keeps counting truthfully.

**By:** [[agent]]
**Date:** 2026-08-27T12:08:55.725139Z
**Area:** [[project|project]]
