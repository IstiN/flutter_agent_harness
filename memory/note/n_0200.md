---
id: "n_0200"
type: "note"
title: "fa v0.1.233 (0.1.233 release chain launched): (1) schedule_message SHIPPED — ScheduledMessageQueue takes repo/root as FACTORIES (late-init safe: tool list built before _fabricRepository in AgentCli ctor); start() best-effort (unwritable root disables, never crashes boot); schedule()'s own timer delivers — tests must assert inbox, not race deliverDue; wired in agent_cli.dart (queue next to _fabricRepository, root=<sessionRoot>/<slug>/messages, unawaited start() after memory maintenance trigger) + agent_service.dart (fabricRepo hoisted, shared). (2) bash `stdin` param — ShellExecOptions.stdinData written to process.stdin then closed (foreground/yield-job/background all). (3) crash hardening: vendor dart_tui wraps model.update in try/catch (runCmd already guarded); _switchCrossProviderModel try/catch notice; terminalColumns/Rows TTY fallback 80/24 in bin/fah.dart; sendBusy REFCOUNTED in FaTuiController (mid-stream slash submits no longer kill spinner/sticky). (4) flutter_agent_memory 0.1.0 wired as GIT dep (ref v0.1.0, eb68d7) — pub.dev still 0.0.6 (owner must dart publish --force once / configure trusted publisher; USER ACTION pending — then flip fa to hosted dep because git deps block fa's own pub.dev publish). Memory agent acked; my next memory cards: deleteRecord/tombstone migration, `memory` role, graph screen, multi-root; fa_llm timeout+cancel noted for its maintainer. Known deferrals: shell stdin UX = ask-tool flow (agent asks user, feeds stdin), deeper menu-during-stream UX (refcount fixed the state corruption; flows already concurrent)."
author: "agent"
date: "2026-08-28T07:08:45.611047Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: [schedule-message, bash-stdin, crash-fix, memory-0.1.0, v0.1.233]
---


# Note: n_0200

fa v0.1.233 (0.1.233 release chain launched): (1) schedule_message SHIPPED — ScheduledMessageQueue takes repo/root as FACTORIES (late-init safe: tool list built before _fabricRepository in AgentCli ctor); start() best-effort (unwritable root disables, never crashes boot); schedule()'s own timer delivers — tests must assert inbox, not race deliverDue; wired in agent_cli.dart (queue next to _fabricRepository, root=<sessionRoot>/<slug>/messages, unawaited start() after memory maintenance trigger) + agent_service.dart (fabricRepo hoisted, shared). (2) bash `stdin` param — ShellExecOptions.stdinData written to process.stdin then closed (foreground/yield-job/background all). (3) crash hardening: vendor dart_tui wraps model.update in try/catch (runCmd already guarded); _switchCrossProviderModel try/catch notice; terminalColumns/Rows TTY fallback 80/24 in bin/fah.dart; sendBusy REFCOUNTED in FaTuiController (mid-stream slash submits no longer kill spinner/sticky). (4) flutter_agent_memory 0.1.0 wired as GIT dep (ref v0.1.0, eb68d7) — pub.dev still 0.0.6 (owner must dart publish --force once / configure trusted publisher; USER ACTION pending — then flip fa to hosted dep because git deps block fa's own pub.dev publish). Memory agent acked; my next memory cards: deleteRecord/tombstone migration, `memory` role, graph screen, multi-root; fa_llm timeout+cancel noted for its maintainer. Known deferrals: shell stdin UX = ask-tool flow (agent asks user, feeds stdin), deeper menu-during-stream UX (refcount fixed the state corruption; flows already concurrent).

**By:** [[agent]]
**Date:** 2026-08-28T07:08:45.611047Z
**Area:** [[project|project]]
