---
id: "n_0304"
type: "note"
title: "Open follow-up on PR #1 (ChatGPT/Codex provider): requested multi-account parity with CodeMie/Copilot as a same-PR fix (not backlog). Issue-level comment: https://github.com/IstiN/flutter_agent_harness/pull/1#issuecomment-5468584996 (2026-08-30).

Current singleton shape at PR #1 head 5dc1f97f:
- lib/src/cli/provider_commands.dart:1023 keyName = spec.apiKeyEnvNames.first (singleton CHATGPT_OAUTH_CREDENTIALS)
- lib/src/cli/provider_commands.dart:928-946 _runChatGptOAuthCommand — no account picker
- lib/src/cli/provider_commands.dart:1068-1077 _persistChatGptCredentials — same singleton key on refresh
- flutter_app/lib/services/chatgpt_oauth_flow.dart:85 const name = 'ChatGPT' hardcoded; L96 matches by baseUrl only

Asks in the comment: per-entry key (CustomProviderRegistry.keyNameFor with providerName, mirroring CodeMie), account picker on second OAuth (mirroring _runCopilotConnectCommand L603-633), guided model pick only for new accounts (CodeMie sameAccount check L1179), flutter app parity (drop const name, match by name+baseUrl).

Reference implementations: _applyCodeMieSsoCredentials (provider_commands.dart:1142-1208) and runCopilotConnectFlow (flutter_app/lib/services/copilot_connect_flow.dart — comment 'Multiple accounts are first-class, like the CLI')."
author: "agent"
date: "2026-08-30T12:07:46.796196Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa-providers", "multi-account", "codex", "chatgpt", "pr-followup"]
---


# Note: n_0304

Open follow-up on PR #1 (ChatGPT/Codex provider): requested multi-account parity with CodeMie/Copilot as a same-PR fix (not backlog). Issue-level comment: https://github.com/IstiN/flutter_agent_harness/pull/1#issuecomment-5468584996 (2026-08-30).

Current singleton shape at PR #1 head 5dc1f97f:
- lib/src/cli/provider_commands.dart:1023 keyName = spec.apiKeyEnvNames.first (singleton CHATGPT_OAUTH_CREDENTIALS)
- lib/src/cli/provider_commands.dart:928-946 _runChatGptOAuthCommand — no account picker
- lib/src/cli/provider_commands.dart:1068-1077 _persistChatGptCredentials — same singleton key on refresh
- flutter_app/lib/services/chatgpt_oauth_flow.dart:85 const name = 'ChatGPT' hardcoded; L96 matches by baseUrl only

Asks in the comment: per-entry key (CustomProviderRegistry.keyNameFor with providerName, mirroring CodeMie), account picker on second OAuth (mirroring _runCopilotConnectCommand L603-633), guided model pick only for new accounts (CodeMie sameAccount check L1179), flutter app parity (drop const name, match by name+baseUrl).

Reference implementations: _applyCodeMieSsoCredentials (provider_commands.dart:1142-1208) and runCopilotConnectFlow (flutter_app/lib/services/copilot_connect_flow.dart — comment 'Multiple accounts are first-class, like the CLI').

**By:** [[agent]]
**Date:** 2026-08-30T12:07:46.796196Z
**Area:** [[project|project]]
