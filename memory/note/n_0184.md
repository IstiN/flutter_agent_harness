---
id: "n_0184"
type: "note"
title: "fa v0.1.231/0.1.232 (main, fa 0.1.232 installed): (1) output flush interval 50ms->16ms (~60fps) — traced real huge session showed p50 frame build 37µs / max 1.6ms / ZERO full rebuilds, so coalescing was the visible chunkiness of streamed thinking; (2) grown-tail rollback now truncates _formatted/_rows/_starts/_srcFmtStarts/_srcRowStarts IN PLACE (length=) instead of sublist+spread — removed the last session-length-proportional per-chunk cost (user's 'long-open session degrades' intuition pointed here; all caches verified bounded: width cache 8192 entries eviction, sticky content-keyed, wrap single-slot). Table-streaming test added (transcript_markdown_perf_test) proving tables re-walk only the table, never full rebuild (debugFullRebuilds static counter exists for diagnosis). User pane traces via FA_TUI_TRACE=/tmp/pane_trace.jsonl; their session reproduces only mild lag now, much better than baseline."
author: "agent"
date: "2026-08-27T21:37:54.538643Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa", "v0.1.231", "v0.1.232", "perf", "trace"]
---


# Note: n_0184

fa v0.1.231/0.1.232 (main, fa 0.1.232 installed): (1) output flush interval 50ms->16ms (~60fps) — traced real huge session showed p50 frame build 37µs / max 1.6ms / ZERO full rebuilds, so coalescing was the visible chunkiness of streamed thinking; (2) grown-tail rollback now truncates _formatted/_rows/_starts/_srcFmtStarts/_srcRowStarts IN PLACE (length=) instead of sublist+spread — removed the last session-length-proportional per-chunk cost (user's 'long-open session degrades' intuition pointed here; all caches verified bounded: width cache 8192 entries eviction, sticky content-keyed, wrap single-slot). Table-streaming test added (transcript_markdown_perf_test) proving tables re-walk only the table, never full rebuild (debugFullRebuilds static counter exists for diagnosis). User pane traces via FA_TUI_TRACE=/tmp/pane_trace.jsonl; their session reproduces only mild lag now, much better than baseline.

**By:** [[agent]]
**Date:** 2026-08-27T21:37:54.538643Z
**Area:** [[project|project]]
