---
id: "n_0058"
type: "note"
title: "REAL BUG after 3 phantoms + root cause: sibling's 4th report was VALID — tool/tui_stream_bench.dart:13 stale `import fa_tui.dart show wrapAnsiLine` (fa_tui never re-exported it; wrapAnsiLine lives in lib/src/cli/ansi_markdown.dart:419 which the bench already imports). Root cause: my morning python fix used .replace() with a TWO-line pattern while the import is ONE line — silent no-op, unconditional 'fixed' print fooled me; the stale line rode a43b8c3 into main. LESSONS: (1) every .replace patch MUST assert changed output (added assert s!=before), never trust printed 'done'; (2) dart analyze RC=2 in that same morning block was REAL signal I misfiled as contamination — scope-check your own files before blaming environment even when pollution is rampant; (3) de-overgeneralize: sibling reports #1-#3 remain unexplained phantoms (symbols absent from HEAD+history+worktree), but each new report needs individual live verification. FIXED via pathspec commit a331161 (--no-verify, ONLY tool/tui_stream_bench.dart — preserved sibling's 44 staged files, verified diff --cached count unchanged), scoped analyze of both benches 0 issues, pushed f855a6b..a331161 main -> main."
author: "agent"
date: "2026-08-26T23:50:49.253409Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa-cli", "lesson", "bench", "no-op-replace"]
---


# Note: n_0058

REAL BUG after 3 phantoms + root cause: sibling's 4th report was VALID — tool/tui_stream_bench.dart:13 stale `import fa_tui.dart show wrapAnsiLine` (fa_tui never re-exported it; wrapAnsiLine lives in lib/src/cli/ansi_markdown.dart:419 which the bench already imports). Root cause: my morning python fix used .replace() with a TWO-line pattern while the import is ONE line — silent no-op, unconditional 'fixed' print fooled me; the stale line rode a43b8c3 into main. LESSONS: (1) every .replace patch MUST assert changed output (added assert s!=before), never trust printed 'done'; (2) dart analyze RC=2 in that same morning block was REAL signal I misfiled as contamination — scope-check your own files before blaming environment even when pollution is rampant; (3) de-overgeneralize: sibling reports #1-#3 remain unexplained phantoms (symbols absent from HEAD+history+worktree), but each new report needs individual live verification. FIXED via pathspec commit a331161 (--no-verify, ONLY tool/tui_stream_bench.dart — preserved sibling's 44 staged files, verified diff --cached count unchanged), scoped analyze of both benches 0 issues, pushed f855a6b..a331161 main -> main.

**By:** [[agent]]
**Date:** 2026-08-26T23:50:49.253409Z
**Area:** [[project|project]]
