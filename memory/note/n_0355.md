---
id: "n_0355"
type: "note"
title: "Reset apps feature (2026-08-30, flutter_agent@4d65a635): AppsStore.resetToFactory() — wipes EVERY app dir under apps/ WITH storage.json (unlike removeWidget), plus .installed.json/.demo_seeds.json/.catalog_cache_v2.json explicitly (dotfiles may not list), then reseeds bundled demos; returns removed-dir count. Settings row 'Reset apps' (ResetAppsSection after HomeGridSection, gated service!=null, destructive red button) → warning dialog (icon + \"deletes every widget you downloaded or created WITH ALL ITS DATA, demos restored, irreversible\") → snackbar with count. l10n keys settingsResetApps*(EN/RU, count placeholder int). Test: clean-worktree run 26/26 (fake assets all share one manifest id — assert DIRS not ids). New unblock pattern for a shared tree broken by another agent's uncommitted refactor: `git worktree add /tmp/fa_clean HEAD` + copy MY modified files + touch flutter_app/.env (pubspec asset) + flutter pub get + run tests there; notify the agent via inbox instead of touching their files; pull --rebase --autostash preserves their WIP when remote only changed release files."
author: "agent"
date: "2026-08-30T16:09:10.337963Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "settings", "reset-apps", "worktree"]
---


# Note: n_0355

Reset apps feature (2026-08-30, flutter_agent@4d65a635): AppsStore.resetToFactory() — wipes EVERY app dir under apps/ WITH storage.json (unlike removeWidget), plus .installed.json/.demo_seeds.json/.catalog_cache_v2.json explicitly (dotfiles may not list), then reseeds bundled demos; returns removed-dir count. Settings row 'Reset apps' (ResetAppsSection after HomeGridSection, gated service!=null, destructive red button) → warning dialog (icon + "deletes every widget you downloaded or created WITH ALL ITS DATA, demos restored, irreversible") → snackbar with count. l10n keys settingsResetApps*(EN/RU, count placeholder int). Test: clean-worktree run 26/26 (fake assets all share one manifest id — assert DIRS not ids). New unblock pattern for a shared tree broken by another agent's uncommitted refactor: `git worktree add /tmp/fa_clean HEAD` + copy MY modified files + touch flutter_app/.env (pubspec asset) + flutter pub get + run tests there; notify the agent via inbox instead of touching their files; pull --rebase --autostash preserves their WIP when remote only changed release files.

**By:** [[agent]]
**Date:** 2026-08-30T16:09:10.337963Z
**Area:** [[project|project]]
