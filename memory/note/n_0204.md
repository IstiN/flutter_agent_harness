---
id: "n_0204"
type: "note"
title: "CORRECTION to copilot goal memories (goal/copilot_provider.md, commit 722d650c): (1) cross-account fallback is REJECTED — 429/limit on one account must never silently switch to another entry; (2) tokens are Keychain-only: GitHub token per named entry stored exclusively in Keychain-backed secure store (SecureKeyStore / app secure storage), short-lived Copilot token stays in memory, config.yaml/JSONL sessions/logs never carry tokens, env FA_KEY_COPILOT_<NAME> is the CI-only exception; (3) TDD is mandatory — the goal doc has a per-layer test-first plan (fa_llm unit via http.testing.MockClient, harness catalog/stream-function/CLI line-mode flows, app widget tests, integration tag for live checks). Note: commit ff7cee96 was partial (edit-tool stale-cache race); 722d650c contains the full state."
author: "agent"
date: "2026-08-28T07:49:27.199569Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "copilot", "fa_llm", "multi-account", "keychain", "tdd", "correction"]
---


# Note: n_0204

CORRECTION to copilot goal memories (goal/copilot_provider.md, commit 722d650c): (1) cross-account fallback is REJECTED — 429/limit on one account must never silently switch to another entry; (2) tokens are Keychain-only: GitHub token per named entry stored exclusively in Keychain-backed secure store (SecureKeyStore / app secure storage), short-lived Copilot token stays in memory, config.yaml/JSONL sessions/logs never carry tokens, env FA_KEY_COPILOT_<NAME> is the CI-only exception; (3) TDD is mandatory — the goal doc has a per-layer test-first plan (fa_llm unit via http.testing.MockClient, harness catalog/stream-function/CLI line-mode flows, app widget tests, integration tag for live checks). Note: commit ff7cee96 was partial (edit-tool stale-cache race); 722d650c contains the full state.

**By:** [[agent]]
**Date:** 2026-08-28T07:49:27.199569Z
**Area:** [[project|project]]
