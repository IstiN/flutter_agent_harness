---
id: "n_0059"
type: "note"
title: "PERF ROUND STATUS 'everything in main' CONFIRMED: local main == origin/main at b118289 (rev-list 0 0). On remote, full chain: a43b8c3 (incremental transcript engine ×729) → 85d6d99 (open-table invariant) → merge 3e13754 → f855a6b (+v0.1.211 integration) → a331161 (bench import real-fix after valid sibling report #4; first three remain unexplained phantoms). Then remote-only b118289 chore(release): v0.1.212 (CHANGELOG+pubspec only), fast-forwarded locally (zero path overlap with sibling's staged set). Local INSTALL rebuilt CLEAN: temp detached worktree at exactly b118289 + install_local.sh from there => ~/.local/bin/fa = v0.1.212 with all perf work; note: first rebuild attempt accidentally compiled SHARED DIRTY tree (sleeping agents' live WIP under lib/cli incl. agent_cli.dart/fa_tui.dart/ansi_markdown.dart/ new tui_text_width.dart extraction) — lesson: in this multi-agent tree ALWAYS build installs from a fresh detached worktree, never the shared checkout. Sibling slim-bundle commit (44 staged deletions) still not landed as of now. NEXT OPTIMIZATION in queue (docs/performance-cli-tui.md #1): AgentCli._persistMessages batching — plan: separate isolated worktree branch perf/persist-batch off main, TDD (order+parity+single-write contract on session storage), pathspec-merge back; avoids collisions entirely with anyone's shared-tree edits."
author: "agent"
date: "2026-08-27T06:34:30.766868Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa-cli", "performance", "status"]
---


# Note: n_0059

PERF ROUND STATUS 'everything in main' CONFIRMED: local main == origin/main at b118289 (rev-list 0 0). On remote, full chain: a43b8c3 (incremental transcript engine ×729) → 85d6d99 (open-table invariant) → merge 3e13754 → f855a6b (+v0.1.211 integration) → a331161 (bench import real-fix after valid sibling report #4; first three remain unexplained phantoms). Then remote-only b118289 chore(release): v0.1.212 (CHANGELOG+pubspec only), fast-forwarded locally (zero path overlap with sibling's staged set). Local INSTALL rebuilt CLEAN: temp detached worktree at exactly b118289 + install_local.sh from there => ~/.local/bin/fa = v0.1.212 with all perf work; note: first rebuild attempt accidentally compiled SHARED DIRTY tree (sleeping agents' live WIP under lib/cli incl. agent_cli.dart/fa_tui.dart/ansi_markdown.dart/ new tui_text_width.dart extraction) — lesson: in this multi-agent tree ALWAYS build installs from a fresh detached worktree, never the shared checkout. Sibling slim-bundle commit (44 staged deletions) still not landed as of now. NEXT OPTIMIZATION in queue (docs/performance-cli-tui.md #1): AgentCli._persistMessages batching — plan: separate isolated worktree branch perf/persist-batch off main, TDD (order+parity+single-write contract on session storage), pathspec-merge back; avoids collisions entirely with anyone's shared-tree edits.

**By:** [[agent]]
**Date:** 2026-08-27T06:34:30.766868Z
**Area:** [[project|project]]
