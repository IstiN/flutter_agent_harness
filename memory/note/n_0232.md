---
id: "n_0232"
type: "note"
title: "fa app-suite repair round (all under main checkout, ungated-clone-era debts): the flutter_app suite had 14 red tests+guards, ALL from other agents' hook-skipped commits — fixed in 0.1.239: (1) agent_service compaction-failure test expected 'transcript untouched' but the emergency _localTrimFallback (0.1.219, deliberate) honestly trims in-memory with a '[context trimmed locally…' marker while the session file keeps every record — test now asserts the marker + full records on disk; (2) session_chat_sheet drawer tests: FlutterSessionManager.openSession completes only over REAL async hops (widget-test fake clock doesn't advance it) — bounded drain loop `for(i<50 && activeId!=target) { await tester.runAsync(() => Future.delayed(10ms)); await pumpAndSettle(); }` after the tap; (3) map goldens: commit 4ea43d4e slimmed the bundle (map demo → fa_widgets catalog) — mapGolden + the controls-menu test now markTestSkipped when assets/apps/map/manifest.json is absent; (4) cli_visual ctrl+s test: shell-job ids are now sh-<n>-<uniq> — parse RegExp 'sh-[A-Za-z0-9-]+' from the /tasks screen and cancel the exact id (bare 'sh-1' → 'unknown job'); (5) cli_visual /agents inbox test: sessions live under <home>/Library/Group Containers/group.dev.fa1.shared/fa/sessions (macOS default, bin/fah.dart _defaultSessionRoot prefers it when writable) — seed mail by searching both roots; (6) session_reuse: aa770722 made corrupt JSONL lines QUARANTINED (dropped + .corrupt sidecar), not fatal — test updated to 'loads with junk quarantined'; (7) WidgetsCatalogSheet failed golden_guard (no golden) + l10n_guard ('Install failed: $error', 'Retry'×2 hardcoded) — added widgetsCatalogInstallFailed/Offline/LoadFailed/Retry keys (en+ru), context.l10n routing (import l10n_ext.dart!), new test/golden/widgets_catalog_golden_test.dart (MockClient catalog, pumpGolden+expectGolden, 420x700 in goldenSizeTall) registered in golden_guard _coverage; gen-l10n regenerates app_localizations*.dart. Regenerated goldens MUST be eyeballed (apps_grid.png = 15 demo tiles, clean). Full app suite: 1356 passed / 44 skipped / 0 failed."
author: "agent"
date: "2026-08-28T11:13:28.116431Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: [fa, app-tests, golden, l10n, guards]
---


# Note: n_0232

fa app-suite repair round (all under main checkout, ungated-clone-era debts): the flutter_app suite had 14 red tests+guards, ALL from other agents' hook-skipped commits — fixed in 0.1.239: (1) agent_service compaction-failure test expected 'transcript untouched' but the emergency _localTrimFallback (0.1.219, deliberate) honestly trims in-memory with a '[context trimmed locally…' marker while the session file keeps every record — test now asserts the marker + full records on disk; (2) session_chat_sheet drawer tests: FlutterSessionManager.openSession completes only over REAL async hops (widget-test fake clock doesn't advance it) — bounded drain loop `for(i<50 && activeId!=target) { await tester.runAsync(() => Future.delayed(10ms)); await pumpAndSettle(); }` after the tap; (3) map goldens: commit 4ea43d4e slimmed the bundle (map demo → fa_widgets catalog) — mapGolden + the controls-menu test now markTestSkipped when assets/apps/map/manifest.json is absent; (4) cli_visual ctrl+s test: shell-job ids are now sh-<n>-<uniq> — parse RegExp 'sh-[A-Za-z0-9-]+' from the /tasks screen and cancel the exact id (bare 'sh-1' → 'unknown job'); (5) cli_visual /agents inbox test: sessions live under <home>/Library/Group Containers/group.dev.fa1.shared/fa/sessions (macOS default, bin/fah.dart _defaultSessionRoot prefers it when writable) — seed mail by searching both roots; (6) session_reuse: aa770722 made corrupt JSONL lines QUARANTINED (dropped + .corrupt sidecar), not fatal — test updated to 'loads with junk quarantined'; (7) WidgetsCatalogSheet failed golden_guard (no golden) + l10n_guard ('Install failed: $error', 'Retry'×2 hardcoded) — added widgetsCatalogInstallFailed/Offline/LoadFailed/Retry keys (en+ru), context.l10n routing (import l10n_ext.dart!), new test/golden/widgets_catalog_golden_test.dart (MockClient catalog, pumpGolden+expectGolden, 420x700 in goldenSizeTall) registered in golden_guard _coverage; gen-l10n regenerates app_localizations*.dart. Regenerated goldens MUST be eyeballed (apps_grid.png = 15 demo tiles, clean). Full app suite: 1356 passed / 44 skipped / 0 failed.

**By:** [[agent]]
**Date:** 2026-08-28T11:13:28.116431Z
**Area:** [[project|project]]
