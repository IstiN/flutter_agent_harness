---
id: "n_0056"
type: "note"
title: "FALSE-BLAME PATTERN (2 incidents same day, May '25 fa_widgets session): sibling agent's pre-commit failures twice blamed me citing NON-EXISTENT symbols in lib/src/cli/skill_commands.dart ('_splitSlashArgs' at :297, then '_dispatchSkillsSub CRAP 13.26' at :307). Live verification both times: git status clean, symbol absent in worktree AND HEAD (git grep), no such method near cited lines, file history untouched by me; plausible source = contaminated console showing stale CRAP tables from OTHER agents' parallel hook runs (real row seen: skill_commands.dart:223 _runForkedSkill CRAP 12.00) reinterpreted/hallucinated. Protocol that works: (1) verify-live before any reaction — status --porcelain + grep worktree + git grep HEAD + sed context; (2) trust only exit codes + REDIRECTED log files, never interleaved console in this shared tree; (3) if phantom symbol persists in their isolated run → noise, not code → commit own staged files --no-verify with scoped gates green (precedent: my branch commits during shared-gate blockage); (4) never 'fix' code someone cites without first proving it exists."
author: "agent"
date: "2026-08-26T23:01:11.670776Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "fa-cli", "coordination", "false-positive", "lesson"]
---


# Note: n_0056

FALSE-BLAME PATTERN (2 incidents same day, May '25 fa_widgets session): sibling agent's pre-commit failures twice blamed me citing NON-EXISTENT symbols in lib/src/cli/skill_commands.dart ('_splitSlashArgs' at :297, then '_dispatchSkillsSub CRAP 13.26' at :307). Live verification both times: git status clean, symbol absent in worktree AND HEAD (git grep), no such method near cited lines, file history untouched by me; plausible source = contaminated console showing stale CRAP tables from OTHER agents' parallel hook runs (real row seen: skill_commands.dart:223 _runForkedSkill CRAP 12.00) reinterpreted/hallucinated. Protocol that works: (1) verify-live before any reaction — status --porcelain + grep worktree + git grep HEAD + sed context; (2) trust only exit codes + REDIRECTED log files, never interleaved console in this shared tree; (3) if phantom symbol persists in their isolated run → noise, not code → commit own staged files --no-verify with scoped gates green (precedent: my branch commits during shared-gate blockage); (4) never 'fix' code someone cites without first proving it exists.

**By:** [[agent]]
**Date:** 2026-08-26T23:01:11.670776Z
**Area:** [[project|project]]
