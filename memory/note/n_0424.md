---
id: "n_0424"
type: "note"
title: "jsr widget bridge-guard pattern: widgets calling jsr.fa.* must guard in the ENTRY functions (bootstrap/load/refresh — IIFE tails call them directly), not just in render() — a render()-only guard never runs because the bridge TypeError throws first. Missing-bridge stub visual language: icon chip + \"Runs in the Fa app\" + muted detail (same as jsr's 3D \"preview unavailable\" and stocks \"Quotes unavailable\" stubs)."
author: "agent"
date: "2026-08-31T15:32:19.961106Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: [jsr-bridge, guard-pattern, web-preview, stub]
---


# Note: n_0424

jsr widget bridge-guard pattern: widgets calling jsr.fa.* must guard in the ENTRY functions (bootstrap/load/refresh — IIFE tails call them directly), not just in render() — a render()-only guard never runs because the bridge TypeError throws first. Missing-bridge stub visual language: icon chip + "Runs in the Fa app" + muted detail (same as jsr's 3D "preview unavailable" and stocks "Quotes unavailable" stubs).

**By:** [[agent]]
**Date:** 2026-08-31T15:32:19.961106Z
**Area:** [[project|project]]
