# ⚠️ Merge Conflicts — Resolve Before Rework

This branch has conflicts with `main`. 4 file(s) contain conflict markers:

- `input/gh-671/ticket.json`
- `input/gh-671/ticket.md`
- `input/ticket.md`
- `outputs/response.md`

## Resolution Steps

1. Open each conflicting file and resolve the `<<<<<<<` / `=======` / `>>>>>>>` markers
2. Stage each resolved file: `git add <file>`
3. Once all conflicts are resolved, proceed with fixes from `pr_discussions.md`

**Do NOT run `git commit` or `git merge --abort`** — the commit and push are handled automatically.
