name: xterm
description: Pure-Dart terminal emulator core (vendored from yoloit's fork).
version: 4.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  convert: ^3.0.0
  meta: ^1.3.0
  quiver: ^3.0.0
  equatable: ^2.0.3

dev_dependencies:
  test: ^1.25.0
