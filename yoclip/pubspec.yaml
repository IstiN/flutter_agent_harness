name: fa_promo
description: Fa — AI Agent promo video (yoclip V2 project).
publish_to: none

version: 0.1.0+1

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  yoclip_core:
    path: ../../yoclip/packages/yoclip_core
  yoclip_encoder:
    path: ../../yoclip/packages/yoclip_encoder

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - yoclip.yaml
    - assets/audio/
    - assets/models/
    - assets/models/Textures/
